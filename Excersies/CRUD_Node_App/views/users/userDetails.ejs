<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>User Details</title>
    <link rel="stylesheet" href="/css/homepage.css"> <!-- Your custom CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
    <style>
        body {
            background-color: darkslategrey; /* Light background */
            height: 100vh;
            display: flex;
            justify-content: center;
            align-items: center;
        }
        .form-container {
            background-color: lightgrey;
            padding: 30px;
            border-radius: 8px;
            box-shadow: 0 0 15px rgba(0, 0, 0, 0.1);
            width: 400px; /* Adjust width as needed */
        }
    </style>
</head>
<body>
    <div class="form-container">
        <h2 class="text-center">User Details</h2>
        <td style="width: 150px; text-align: center;"> <!-- Adjust width as needed -->
            <img src="<%= foundUser.image %>" alt="User Image" style="max-width: 100%; max-height: 80px; display: block; margin: auto;">
        </td>
        <form action="" method="POST" autocomplete="off">
            <div class="mb-3">
                <label for="name" class="form-label">Name</label>
                <input type="text" class="form-control border border-black" id="name" name="name" required value="<%= foundUser.name %>" readonly>
            </div>
            <div class="mb-3">
                <label for="age" class="form-label">Age</label>
                <input type="number" class="form-control border border-black" id="age" name="age" value="<%= foundUser.age %>" readonly>
            </div>
            <div class="mb-3">
                <label for="mail" class="form-label">E-mail</label>
                <input type="email" class="form-control border border-black" id="mail" name="mail" required value="<%= foundUser.mail %>" readonly>
            </div>
            <div class="mb-3">
                <label for="phone" class="form-label">Phone</label>
                <input type="tel" class="form-control border border-black" id="phone" name="phone" value="<%= foundUser.phone %>" readonly>
            </div>
            <div class="mb-3">
                <label for="image" class="form-label">Image URL</label>
                <input type="text" class="form-control border border-black" id="image" name="image" value="<%= foundUser.image %>" readonly>
            </div>
        </form>
        <a href="/users" class="btn btn-secondary w-100 mt-2">Home</a>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js" integrity="sha384-YvpcrYf0tY3lHB60NNkmXc5s9fDVZLESaAA55NDzOxhy9GkcIdslK1eN7N6jIeHz" crossorigin="anonymous"></script>
</body>
</html>
